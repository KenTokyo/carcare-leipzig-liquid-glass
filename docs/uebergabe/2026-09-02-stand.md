# Übergabestand 2026-09-02

Alles Genannte liegt auf `main` und ist gepusht. Keine offenen Branches mit
halbfertigem Stand.

---

## Was heute auf `main` gelandet ist

### Schleife 1, Paket A — globale Textkorrekturen (1.1 – 1.4)
- Gründungsjahr durchgehend **„seit 1998"**, keine relativen Altersangaben mehr.
  Auch `foundingDate` im JSON-LD — das Feld, das KI-Antwortsysteme bei „wie alt
  ist der Betrieb" übernehmen.
- Betriebsfläche durchgehend **„über 3.000 m²"**, Einheit `qm` abgeschafft.
- **„CarCare Center"** statt „CarCare"; Aussagesätze in der ersten Person Plural.
  Leitregel, jetzt in `CLAUDE.md`: *Der Name darf stehen, aber nie als Subjekt
  eines Verbs in der dritten Person.*
- Zwei dokumentierte Ausnahmen: die juristische Firmierung „BS CarCare GmbH"
  und die dritte Person in JSON-LD-Beschreibungen.

### FAQ: eine Quelle, abgeleitetes Schema, zwei Wächter
- `data/faqs.ts` ist die einzige Quelle aller FAQ-Inhalte (19 Routen, 97 Einträge).
  `PageFAQ` nimmt eine Route, kein Array — Doppelpflege ist nicht mehr darstellbar.
- `scripts/check-faq.mjs` (**prebuild**) bricht ab bei Markup ohne sichtbaren Block.
- `scripts/check-faq-html.mjs` (**postbuild**) bricht ab, wenn ausgezeichneter
  FAQ-Text nicht im ausgelieferten HTML steht.
- Das FAQ-Akkordeon hängt geschlossene Antworten nicht mehr aus dem DOM aus;
  Barrierefreiheit dabei verbessert (`h3`-Trigger, `aria-controls`, `inert`).

### Schleife 1, Paket B — Informationsarchitektur Aufbereitung
- Neue Seiten **`/aussenaufbereitung-leipzig`** und **`/innenaufbereitung-leipzig`**
  (Prerender 23 → 25 Routen).
- Kachel „Lackaufbereitung" entfernt, ihr Inhalt geht auf der Außenseite auf.
- Bild-Rochade umgesetzt; Bildzuordnung einquellig in `aufbereitungKacheln`.
- Ozon und Heißvernebelung direkt unter die Pflegepakete, zusätzlich als
  „Optional buchbar" auf der Innenaufbereitungsseite.
- Scheinwerferaufbereitung entfernt (rechtlich heikel) — war genau eine
  Fundstelle, nie im JSON-LD.
- „Data Motive" auf der Geschäftskundenseite.

### Werkzeug
- `@types/react` / `@types/react-dom` installiert. Vorher prüfte `tsc` **keine
  einzige JSX-Property** — „Typecheck grün" war für Komponenten-Code eine leere
  Aussage.
- `strict: true` aktiviert. Beides ohne einen einzigen Fehler im Bestand.

---

## Was als Nächstes ansteht: Paket C

`docs/backlog/schleife-1.md`, Paket C — Serviceseiten-Layout vereinheitlichen.

| Nr. | Aufgabe |
|---|---|
| **1.14** | Layout durchgehend vereinheitlichen (Hintergrundfoto oben, weich übergehend in Weiß) für acht Seiten: Neu- & Reparaturlackierung, Smart Repair, Dellenentfernung, Hagelschadenreparatur, Felgenreparatur, Autoglas & Scheibenfolierung, Leasingrückgabe, Fuhrparkservice |
| **1.15** | Jede Leistung kurz und eigenständig erklären (Ziel: direkte Landung bei Suche „Smart Repair Leipzig") |

**Hinweis aus dem Backlog:** 1.14 ist ein Fall für eine gemeinsame Layout-Komponente,
nicht für acht einzeln angepasste Seiten. Erst Komponente bauen, dann migrieren.

**Vorarbeit aus Paket B, die 1.14 hilft:** `BackdropLayout` leistet genau das
geforderte stehende Hintergrundfoto und ist auf `/fahrzeugaufbereitung-leipzig`,
`/leasingrueckgabe-leipzig` sowie den beiden neuen Seiten im Einsatz. Für 1.14
geht es also eher ums Migrieren der übrigen sechs Seiten als ums Erfinden.
`FeatureGrid` hat seit Paket B einen `tone`-Parameter (`solid` | `translucent`) —
über Foto-Hintergründen ist `translucent` der richtige Wert.

---

## Vier Zulieferungen, die bei André liegen

| Nr. | Was fehlt | Was währenddessen im Code steht |
|---|---|---|
| **1.18** | Liste der Zusatzleistungen fürs Aufbereitungsformular | Die Exklusivleistungs-Abschnitte auf beiden neuen Seiten stehen strukturell mit den belegten Bezeichnungen, aber **ohne Beschreibungstexte**. Im Code markiert als `TODO 1.18 – Beschreibungen ausstehend, Zulieferung André` — per Suche auffindbar. |
| **1.26** | Benefits und Mitarbeiterstimmen für die Karriereseite | nichts vorbereitet, betrifft Paket D |
| **1.27** | Weitere Meilensteine für die Zeitleiste auf `/ueber-uns` | Zeitleiste führt aktuell nur „1998 — Gründung" und „Heute" |
| **1.28** | Eigene Bildmotive für Leasingrückgabe und Außenaufbereitung | Beide teilen sich ihr Motiv mit anderen Kacheln, dadurch fallen Dateiname und Einsatzort auseinander. Umbenennen scheidet aus — die Namen sind an ihren anderen Einsatzorten korrekt. |

---

## Offene Optimierungspunkte

**`docs/textkorrekturen-schleife-1/tasks/2026-09-01-paket-a-optimierung-tasks.md`**
— Punkte 1 und 2 erledigt. Offen: 3 (Hero.tsx ist toter Code), 4 (doppeltes
„seit 1998" auf fünf Karten), 5–7 (Meta-Längen).

**`docs/paket-b-aufbereitung/tasks/2026-09-02-paket-b-optimierung-tasks.md`**
— Punkt 1 (🟠 hoch): `DetailingGallery` führt eine zweite, ungeprüfte
Leistungsliste. Solange Platzhalter, folgenlos; **vor dem Einsetzen echter Fotos
konsolidieren.**

---

## Zustand des Arbeitsverzeichnisses

Untracked und bewusst so belassen — fremde Arbeit bzw. Exporte:

- `parallax-scroll-kit/` — portabler Kit-Export
- `docs/parallax-scroll-kit/` — zugehörige Task-Dateien
- `docs/naechste-session/` — To-do-Liste vom 04.08.

Ebenfalls nicht angefasst: die Branches `claude/frosty-chaum-e06c4d` (liegt in
einem eigenen Worktree) und `claude/focused-volhard-993f3e`.

---

## Prüfstand beim Abschluss

`tsc --noEmit` sauber (mit `strict`) · Build 25/25 Prerender ·
`check-faq` 19 Routen, 20 `faqSchema`-Aufrufe · `check-faq-html` 25 Seiten,
224 FAQPage-Texte, alle ausgeliefert.
